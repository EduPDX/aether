"""Domain layer: entities, value objects and domain events.

Pure Python only — this package must never import FastAPI, SQLAlchemy or
any other infrastructure concern. Populated as of the Instance entity
(roadmap v0.1, "folders-only instances").
"""
